terraform {
  required_version = ">= 1.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

# Use tflocal to automatically route to LocalStack
provider "aws" {
  region = var.region
}
